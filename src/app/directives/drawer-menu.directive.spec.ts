import { DrawerMenuDirective } from './drawer-menu.directive';

describe('DrawerMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerMenuDirective();
    expect(directive).toBeTruthy();
  });
});
